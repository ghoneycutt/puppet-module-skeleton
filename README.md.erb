# puppet-module-<%= metadata.name %>
===

[![Build Status](https://travis-ci.org/<%= metadata.author %>/puppet-module-<%= metadata.name %>.png?branch=master)](https://travis-ci.org/<%= metadata.author %>/puppet-module-<%= metadata.name %>)

Brief description here.

===

# Compatibility
---------------
This module is built for use with Puppet v3 (with and without the future
parser) and Puppet v4 with Ruby versions 1.8.7, 1.9.3, 2.0.0 and 2.1.0 on the
following OS families.

* EL 6
* Some other OS

===

# Parameters
------------

example_param
-------------
Boolean to do something awesome.

- *Default*: true
